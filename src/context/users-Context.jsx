import { createContext } from "react";

// 1. create context to use globle
// context : A way to pass data through the component tree 
// without having to pass props  manually at every level .
const UserContext =createContext();

